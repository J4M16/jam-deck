# Changelog

## 0.30.3 — 2026-08-06

- **归档标记改为 Obsidian 隐藏注释**：任务块首尾标记从 `<!-- jam-deck-life-task:... -->`（HTML 注释，阅读/实时预览会显示）改为 `%% jam-deck-life-task:... %%`（Obsidian 原生隐藏注释，预览中不可见），日记观感干净。旧归档中的 `<!-- -->` 标记仍可读取、更新与移除，重写后自动升级为隐藏注释。
- 处理模型签名：MiniMax-M3（WorkBuddy 主对话）

## 0.30.2 — 2026-08-06

- **canvas-embed 引导态新增「新建 Canvas」**：新用户没有画布时，点击引导卡片上的「新建 Canvas」即可创建一个空画布（`未命名.canvas`，重名自动递增）并直接挂载，无需手动准备文件。
- **行尾规范化**：新增 `.gitattributes`（文本 `eol=lf`、ps1 `eol=crlf`），修复 Windows `core.autocrlf` 检出导致工作区文件变 CRLF 后测试断言（按 LF 匹配）失败的问题。
- 处理模型签名：MiniMax-M3（WorkBuddy 主对话）

## 0.30.1 — 2026-08-06

- **默认工作台布局更新**：新 vault 首次打开时的组件排布改为按 Jam 当前布局（时钟 + 音乐 + 快捷方式顶栏，左侧待办/日历/剪贴板，右侧大块原生 Canvas）。组件用稳定 id（`canvas-embed-1` 等），快捷方式列表与 Canvas 文件路径留空（新用户自选），canvas-embed 未配置时显示「选择一个 Canvas」引导态（已有容错）。
- 处理模型签名：MiniMax-M3（WorkBuddy 主对话）

## 0.30.0 — 2026-08-06

- **归档形式可配置 + 统一简单格式（发布计划 P1 体验完善）**：
  - 设置面板「归档路径」段改版：工作/生活各一个「归档形式」下拉（**文件 / 目录**，默认都是文件）+ 对应路径输入，切换时自动显隐。
  - **文件模式**（默认）：归档到单个 Markdown（工作 `Work/工作.md`、生活 `Life/Daily.md`），按 `# YYYY年M月D日` 标题分节。
  - **目录模式**：归档到目录下按日期生成 `YYYY-MM-DD.md`（工作 `Work/工作日记/`、生活 `Life/生活日记/`）。
  - **格式统一**：工作归档从旧四段式（`work-daily-v2` frontmatter + 工作内容/效果图/链接/备注）改为与生活一致的**统一简单块格式**（`- [x] 标题` + 分类/截止/说明/链接/图片 + `<!-- jam-deck-life-task:...-->` 标记），新 kind `work-daily-v3`；工作/生活仅「分类」行不同。
  - **旧数据兼容**：`work-daily-v2` 四段式历史归档仍可读取/恢复/编辑（`archiveFormat: "section-v2"` 保留），不会被新归档覆盖；`getTaskArchiveRef` 兜底判断生活目录模式路径。
  - README 新增「归档格式」章节：两种模式的产物结构 + 任务块标记说明。
- 回归：测试重构——工作归档断言改统一简单块（file 模式单文件 + dir 模式日期文件），新增 dir 模式集成断言与 mode/path getter 断言；旧四段式历史数据迁移断言保留。`npm run verify` 全绿。
- 处理模型签名：MiniMax-M3（WorkBuddy 主对话）

## 0.29.5 — 2026-08-06

- **归档路径可配置（P1-1）**：设置面板新增「工作归档目录」（默认 `Work/工作日记`）与「生活归档文件」（默认 `Life/Daily.md`）两项。`getWorkArchiveDir()` / `getLifeArchivePath()` 读设置、空值回退内置常量，旧 vault 行为完全不变；`ensureVaultFolder` 兜底保留。回归：fixture 补缺省回退 + 自定义路径断言，verify 全绿。
- 处理模型签名：MiniMax-M3（WorkBuddy 主对话）

## 0.29.4 — 2026-08-06

- **仓库重组：Game Deck 剥离为独立仓库**（纯维护，无功能变化）：`game-deck/` 目录、构建/部署脚本与测试迁至独立仓库 `D:\Project\GameDeck`（id `game-deck`，经 git filter-repo 保留其历史）；本仓库只维护 Jam Deck 主插件。package.json 移除 three/esbuild 依赖与全部 `*:game-deck` scripts，`verify` 简化为 check + test；README/AGENTS.md 同步清理；`.gitignore` 新增 `.workbuddy/` 与 `debug-backups/`。
- 处理模型签名：Deepseek-V4-Flash（WorkBuddy 主对话，plan/整合/执行）

## 0.29.3 — 2026-08-06

- **边缘 widget 角点无 sash handle（修 0.29.1 漏的根因）**：`jamDeckCollectLayoutSashes` 之前只把"全局 grid 右/下边界"（`x+w === rightLine` / `y+h === bottomLine`）的 widget 视为 edge，忽略了 widget 自己在网格内部的局部底/右边界（如下方无邻居的 widget bottom）。cross product 因此不会为局部边缘角点生成 xy node——以当前布局为例，canvas-embed `y+h=20` 在 col 8-40 下方无邻居、但不是全局 bottomLine=37，被漏掉，其右下角 (col 41, row 20) 永远没有 sash handle，无法从该角拉伸。修复：改为 per-widget 检测"右/下边界是否有 y/x overlap 邻居"，edge sashes 按 line 分组合并并保留各自 line 字段。回归：verify 全绿。
- 处理模型签名：MiniMax-M3（WorkBuddy 主对话）

## 0.29.2 — 2026-08-06

- **文件夹背叶/前叶提亮**：Jam 反馈当前文件夹颜色偏暗——背叶（backboard 底板）+15% 明度、前叶（磨砂前片）+10% 明度。实现为白混（`color-mix ... 85%/15%`、`90%/10%`）而非 HSL 明度加点：樱粉/月黄/浅红/天蓝的 HSL 明度已超 80%，直接 +15 点会漂成近白丢色相。
  - `--jd-folder-backboard-color`：`color-mix(in srgb, var(--jd-folder-color) 85%, #fff 15%)`。
  - `--jd-folder-front-start/end`：tint 混 10% 白。
  - 色板圆钮（color swatch）同步按 85%/15% 提亮，选色所见即渲染所得。
- 回归：`npm run verify` 全绿（断言锁结构不锁色值）。
- 处理模型签名：Qwen3.8-Max（主代理，WorkBuddy）

## 0.29.1 — 2026-08-06

- **canvas 组件右下角拉伸手柄命中过紧**：sash handle 命中半径 18→24px。原因：canvas-embed 组件内部右下角被 Obsidian 原生 `.canvas-controls`（z-index 100）覆盖，sash 实际生效区仅在 widget 边界外侧 13px 一圈（`width:26; margin:-13 0 0 -13`），18px 半径要求鼠标必须落在 widget 外侧脱壳区，频繁脱靶。扩到 24px 给出 6px 缓冲，hover 更容易命中。
- 处理模型签名：MiniMax-M3（WorkBuddy 主对话）

## 0.29.0 — 2026-08-06

- **文件夹外观还原 NZS4 Figma「文件夹样式」（134:143）**：经 Figma Desktop Bridge 读取六个变体（纸灰/浅红/樱粉/月黄/草绿/天蓝）的精确属性后整体还原。
  - **六色**：`JAM_DECK_CANVAS_FOLDER_COLORS` 换为 Figma 底板实色 `#C1C1C1 / #F7BDB1 / #F0C5DA / #EDD0AE / #BBE0AF / #AFD0E0`；面板 `--jd-folder-panel` 直接用实色（不再 90% 混白）。
  - **前片**：改 Figma 封面语义——单层 tint 渐变（顶 50% 透明 → 底实色）叠磨砂 blur，去掉白色 screen 层与描边；tint 按色号独立注入 `--jd-folder-front-tint`（`#E7E7E7 / #FAC0C0 / #F8CECE / #FBE2BB / #CCF2C0 / #BEE1F3`）。
  - **文字**：label 改 Inter 16px Regular 黑@0.5、count 改 10px Regular 黑@0.3，定位按 Figma y=124/y=110（top 82.67%/73.33%）。
  - **阴影/边**：壳体阴影改 `0 4px 20px rgb(0 0 0 / 0.10)`，描边 token 透明化。
  - **旧数据迁移**：0.19.0/0.28.6 持久化旧色经 `JAM_DECK_CANVAS_FOLDER_LEGACY_COLORS` Map 语义映射到新六色（蓝灰/旧浅红→浅红，旧纸灰→纸灰，草绿/樱粉/月黄/天蓝按色相归位），老文件夹不静默变色。
- 回归：新增六色锁定、front tint 注入、渐变与 Inter 字体栈断言；旧材质/旧定位断言更新为 NZS4 值；`npm run verify` 全绿。
- 处理模型签名：Qwen3.8-Max（主代理，WorkBuddy）

## 0.28.9 — 2026-08-05

- **矮图拖不进文件夹 + 被旧堆叠缩小（追加）**：双重根因。①旧堆叠 `attemptAutoSnap` 的候选用 `getStackItems()`（默认含文件夹成员）——埋在锚点、互相重叠的文件夹成员被当成旧堆叠集群，拖图上去时旧逻辑先把图 `jamDeckNormalizeCanvasStackImage` 缩小再吸附叠上，文件夹加入逻辑被旁路。修复：候选改 `getStackItems(false)`，旧堆叠永不触碰文件夹成员。②`findDropTarget` 对折叠文件夹用 200×180 壳体面积比（交集÷较小面积），宽矮图（如 400×60）交集被壳体 200 宽卡死在 0.5，`> 0.5` 阈值永远达不到。修复：加图标式拖放语义——拖拽中心点落进壳体即命中（ratio=1），与宽高比无关。
- **修复 hover 翻动动画被 WAAPI fill 残留锁死**：`animateFolderPreviewFront` 用 `fill:"both"` 播放前片翻开/合拢动画，动画结束后从未 cancel——WAAPI 的填充效果会持续覆盖 CSS transform，把 front 钉在 `perspective(260px) rotateX(0deg)`，导致 `.jam-deck-canvas-folder:is(:hover, :focus-within) > .front` 的 `rotateX(-30deg)` 规则永远改不动它（preview 打开又关闭一次后 hover 翻动就永久消失）。修复：`finish()` 里对 `runtime.animation` 执行 `cancel()` 再置空，让 front 回到 CSS 值。
- **折叠态原生分组框贴合壳体**：原生 group node 的 bbox 此前取成员堆叠矩形的并集（成员可保留大尺寸），导致分组框比 200×150 壳体视觉大太多、不贴靠。新增 `nativeFolderShellBounds()`（anchor 堆叠中心 ± 100/75 的 200×150 设计基准），折叠态与编组创建的 group bbox 统一用它；展开态仍用原位成员包围盒（囊括全部可见成员）。`renameNativeFolder` 折叠态同步改用 shell bounds。
- **原生分组框数据化（追加）**：group bbox 改 200×180（新常量 `JAM_DECK_NATIVE_GROUP_BASE_HEIGHT`）；`styles.css` 隐藏 `.jam-deck-canvas-leaf .canvas-group`（visibility:hidden + pointer-events:none）——JamDeck 工作台里原生分组框/标签不可见，只保留 JamDeck 壳体视觉；group 节点仍存在于 Canvas 数据（原生 canvas 打开时仍是分组囊括状态）。
- **修复点击不展开 + group 框选择器（追加）**：
  - 分组框隐藏选择器修正：Obsidian 1.13 的 group 渲染 class 是 `.canvas-node canvas-node-group`（不是 `.canvas-group`，后者是工具栏控件组）——`.canvas-node-group` 才命中。
  - 点击不展开根因：`nativeGroupNode` 用 `node.nodeType === "group"` 查找，但 1.13 压缩后 GroupNode 的 `nodeType` 是 undefined（构造函数单字母），group 永远找不到 → `expandNativeFolder` 直接 return false。改为按稳定 `id` 匹配 + `getData().type === "group"` 确认（序列化 type 才是 `"group"`）。`collapseNativeFolder`/`renameNativeFolder`/`ungroup`/整组拖动同步一并修复。
- **展开自动排开（追加，千问审查合入）**：native 展开此前只回到 `positions`（编组前坐标），编组前就重叠的卡片展开后依旧叠着（`.canvas` 文件取证：两成员 x 差 37px）。新增 `nativeExpandTargets()`：positions 成员两两 IoU>0.5 **或中心点在对方矩形内**（大小悬殊时 IoU 不可靠）判定重叠 → 用 `jamDeckCanvasFolderGridLayout` 排全尺寸网格（columns 2/3、gap 24、cellHeight=maxHeight、bounds 原位中心对齐且保证不缩小），展开目标写回 payload.positions 持久化；不重叠则尊重用户手动布局。单成员/缺失/rect 不全/grid 失败一律 fallback 原 positions。折叠态连接点确认已隐藏（DOM 取证）；展开态端点保留（释放交互），排开后自然分散。
- **真打包连线 / 堆叠触发 / 控制条可点（追加，三问题一次修）**：
  - **真打包**：折叠态成员 `visibility:hidden` 不够——连线（edges）仍按数据坐标指向埋点（DOM 取证：canvas 有 6 条 SVG edge，壳体上下出现"连线端点"）。`collapseNativeFolder` 现在把指向成员的 edge 从 `data.edges` 移除、完整数据存 `payload.hiddenEdges`，展开/取消编组恢复；`mutateNodes` 扩展 `edgeChanges` 参数，同一原子事务提交，回滚含 edges。schema 增 `hiddenEdges` 字段。
  - **堆叠触发**：native 折叠态成员埋在锚点，`findDropTarget` 此前用数据层堆叠区判定 50% 重叠（区域比壳体视觉大且可能偏移）→ 拖到壳体上常不触发。改为用 `nativeFolderShellBounds`（壳体视觉 200×180）判定；`updateGroupMembership` 加 native 分支，新成员补 stacked（锚点矩形）+ positions（当前坐标），折叠态加入即移到锚点堆叠。
  - **控制条可点**：展开态控制条挂在 canvas world 层，缩小到 0.4x 时只有 79×16px、close 按钮 8px → 点不到，导致"没有方法折叠回"。给壳体加 `transform: scale(1/canvas.scale)`（origin 底部中心）反向抵消 world 缩放 → 屏幕尺寸恒定 200×40px。
- 回归：`tests/jam-deck-test.js` 新增断言锁定「preview flap WAAPI 必须在 finish 时 cancel 其 fill:both 动画」。

## 0.28.8 — 2026-08-05

- **文件夹缩放露出（修复 1/3）**：`CanvasFolderController.reconcile` 之前在 `validateFolderToolbarLayer` 返回 unsafe 时执行 `restoreFolderOwnedNodes()` 撤销折叠隐藏——重置缩放瞬间触发壳体与工具栏重叠采样异常 → safe=false → 成员暴露。折叠隐藏改为完全由 `group.collapsed` 驱动（`applyFolderRuntimeNodes` 的 `hide` 条件去掉 `view.safe` / `view.shell.hidden`），`updateFolderView` 的 `shell.hidden` 只随展开态翻转；unsafe 时仅告警，不再撤销折叠。**`styles.css` 折叠态成员连接点**新增 `.canvas-node-connection-point { pointer-events:none; opacity:0 }`（折叠/过渡态统一隐藏，展开态恢复）——修复 0.28.6 漏修的连线端点交互。
- **新编组接入原生 Canvas 分组（修复 3/3）**：
  - **Schema v1 增字段**：`native`/`label`/`nativeGroupId`/`positions`/`stacked`（可选，老数据全部缺省）。`jamDeckCanvasFolderSchema` + `folderRecord` + `collectGroups` 同步。
  - **核心数据流**：native 折叠 = 成员坐标移到 `stacked` 锚点堆叠矩形 + 原生 `group`（type:'group'）bbox 覆盖堆叠区 + `payload.collapsed=true`；展开 = 坐标回到 `positions` 原位 + bbox 扩到原位包围盒 + `collapsed=false`。通过一次 `mutateNodes` 原子事务提交（Obsidian 1.13 原生 group 是 `nodes` 里的 `type:'group'` 条目，与成员一并 setData 即可持久化、共享 undo）。
  - **Obsidian API 用法**（运行时探测确认）：`canvas.createGroupNode({pos,size,label,save:false})` 创建实例并进入 `getData()`；`canvas.removeNode(group)` 删除；`group node data = {id,x,y,width,height,type:'group',label}`。`getNativeGroupCapability()` 运行时 gate，能力缺失时自动回退老 preview。
  - **交互语义**：折叠态壳体点击 = 展开（成员释放，可连线/可选中/可拖动，原生 group bbox 变大）；退出 = 壳体控制条上的「收起」按钮（chevron-up，`view.close`）或 Escape（document keydown 拦截），**点空白处不再自动收起**。`onClick` 拦截 native 展开态的空白点击避免误关。
  - **壳体 header 改版**：label span 默认显示 `folder.label`（"文件夹"），native 模式双击 label 弹 `window.prompt` 重命名（同步更新 payload + 原生 group node label）；controls 新增 close 按钮，native 展开态壳体压缩为 200×40 浮条（`.is-expanded.is-native-folder` CSS 控制条样式）浮在 expanded bounds 顶部。
  - **老数据兼容**：0.28.x 旧文件夹（`native` 缺省）继续走 preview 路径，不升级；仅新编组走 native。`reconcile` 的 `collapsed:true` 强制改为 `native ? payload.collapsed : true`。
  - **几何边界**：新加入成员可能没有 stacked 矩形（`updateGroupMembership` 路径），折叠时 fallback 到 anchor 的 stacked rect（不会留在原位露出）。`finishFolderShellDrag` 整组拖动同步 group bbox；`ungroup` native 分支恢复 positions + removeNode + requestSave。
  - **FLIP 动画**：展开/折叠走真节点 WAAPI 过渡（`animateNativeFolderTransition`，从 mutateNodes 前的 screen rect 飞到落定 CSS transform），沿用现有 `transformWithDelta` + `is-jam-deck-folder-transitioning` class。reduced-motion 跳过动画。
- **测试断言更新**：`tests/jam-deck-test.js` schema 13 字段断言 + native/label/positions 解析 + 控件 close 顺序断言。`npm run verify` 全绿。

## 0.28.7 — 2026-08-05

- **屎山清理（大型重构）**：
  - **CSS final cascade 合并**：styles.css 中 25 组完全重复选择器（同名规则 2-6 次定义、末尾覆盖前面——hover 等样式修不好的根因）合并为单一定义，删除前面重复，文件 3300+ → 3235 行。
  - **拆分 5 个超长函数**：TaskDetailModal.onOpen(200行)、renderAiChat(172行)、createFolderView(155行)、renderMusicPlayer(144行)、showPreview(137行)，按区块拆为子方法，行为不变。
  - **controlMusic 确认轮询**：裸 setTimeout 链加 pending id 检查，消除提前确认后的幽灵回调。
  - **rAF helper**：抽取 `jamDeckRequestFrame` 消除 3 处 rAF fallback 重复。
  - **魔法数字常量化**：JAM_DECK_STACK_PREVIEW_CLEANUP_MS、JAM_DECK_FOLDER_FALLBACK_WIDTH/HEIGHT_RATIO。
  - **legacy 数据迁移**：日记 v1→v2 迁移保留并加 TODO（删除会放弃老日记迁移，确认发版周期后再删）。
- 行为不变，全部重构经 `npm run verify` 验证，工作台视觉无破坏。

## 0.28.6 — 2026-08-05

- **文件夹色板更新**：中灰 `#A9A9A9` 换成**天蓝 `#5E9BD6`**；其余五色（绿/紫/黄/粉）**饱和度各自 +30%**（HSL 独立换算）：`#9DBB8E→#9BC287`、`#C69CB8→#CC96BA`、`#D9B36C→#E9B85C`、`#D58C78→#E3846A`。
- **修复前片翻开方向**（打开预览 + hover 两处都改）：均改为**底边固定**（transform-origin 50% 100%）、**顶边向 viewer 翻起**、**顶边显宽**。打开预览：`rotateX(80deg)` + perspective 260px；hover 微翻：`rotateX(30deg)` + perspective 420px。
- **修复折叠文件夹选中范围巨大**：折叠时若真实成员节点处于选中态（选中框 = 成员包围盒，可达 800px+），现在自动 `deselectAll` 清除，选中框回归壳体附近。

## 0.28.5 — 2026-08-05

- 文件夹壳体**面板浓度提升**：`--jd-folder-panel` 的 color-mix 比例由 78% → 90%（底色混入 22% → 10%），选中颜色更扎实。
- **修复文件夹前片翻开方向颠倒**：前片 transform-origin 由底边（50% 100%）改为顶边（50% 0%），与 hover 悬浮方向一致——现在像书页一样绕顶部向上翻开，露出下方成员。

## 0.28.4 — 2026-08-05

- **动画与系统脱钩**：Jam Deck 所有动画不再跟随系统「减少动态效果」（`prefers-reduced-motion`）。删除全部 12 处系统联动 CSS 块，改为插件自己的「动画效果」设置开关（设置页顶部，默认开启）——关闭时给工作台加 `.jam-deck-no-motion` 统一停用动效。JS 不再读系统 media query，改用 DOM class 判断。
- 受影响范围：展开/收起 FLIP、文件夹前片、倒计时翻牌、CD 旋转、拖拽悬浮、推挤过渡等全部动效，默认全开。

## 0.28.3 — 2026-08-05

- **修复展开图糊**：展开卡片不再用 `transform: scale(9)` 放大代理小图（位图级放大导致模糊），改为**卡片直接落在真实布局尺寸**（left/top/width/height = 排列目标），transform 只做"从代理位置飞到排列位置"的过渡动画，结束后无缩放——原图位图（如 1536px）由浏览器重采样到目标尺寸，清晰度恢复。
- 文本卡片字号/内边距同步改为固定 16px（不再反向补偿 transform 缩放）。

## 0.28.2 — 2026-08-04

- 文件夹编组壳体：代表成员（proxy）加 2px 白色描边（border + box-sizing: border-box），跨主题统一可见。
- 文件夹壳体：meta 定位改为 `top: auto; bottom: 26px`，与 controls（图标）底部对齐，文案不再浮在壳体中段。
- **修复点开编组不放大排开**：系统开启「减少动态效果」时，reduced-motion CSS 把展开卡片的 FLIP 目标 transform 强杀为 `none`，卡片永远停在源位置小图叠放。现改为 reduced-motion 下保留最终落位 transform（只去掉过渡动画），展开布局照常生效。

## 0.28.1 — 2026-08-04

- Canvas 悬浮改为**300ms 按住延迟判定**：300ms 内松手视为单击（不悬浮、直接展开堆叠预览）；按住超过 300ms 或拖动越过 5px 阈值才悬浮。单击不再闪现悬浮效果。

## 0.28.0 — 2026-08-04

- Canvas 节点**按住即悬浮**：指针按下（pointerdown）立即抬起，不再等拖过 5px 阈值；松开/取消时恢复。
- 悬浮视觉改为**阴影 + 降 50% 透明度**，去掉原有 `translateY(-6px)` 位移与 `scale(1.018)` 缩放。图片、Canvas 文本与 Markdown 笔记三类节点统一生效。

- 修复 AI 助手"处理中"提示与实际模型不符：提示文案之前用"是否挂了图片"推断模型（有图→千问、无图→DeepSeek），切换到千问后发纯文本仍显示"DeepSeek 处理中"。改为跟随当前模型按钮状态（provider）。

## 0.27.2 — 2026-08-04

- AI 助手贴回画布不再堆叠：新增 `findFreeCanvasRect`——目标位置被现有文本/图片节点占据时，自动向右下方扫描（最多 4 行 × 8 列，带 24px 安全间距）找空位落点。
- 新节点创建后**自动聚焦**：取消全选 → 选中新节点 → `zoomToSelection` 视野拉过去（无该方法时回退 focus）。
- 翻译贴文与 AI 的 `addCanvasText` 操作共用同一路径，一并受益。

## 0.27.1 — 2026-08-04

- 修复 AI 助手图片预览条未载入图片时仍占位显示：`.jam-deck-ai-image-dock` 的 `display: flex` 覆盖了 HTML `[hidden]` 属性，空预览条一直显示。补 `[hidden] { display: none }` 规则，未载入图片时彻底隐藏。

## 0.27.0 — 2026-08-04

- AI 助手支持**拖图进对话框**：从剪贴板组件拖图片卡片、从文件管理器/浏览器拖图片文件、或直接 Ctrl+V 粘贴截图，都能载入为看图上下文（自动切千问）。
- 输入框上方新增**图片预览条**（缩略图 + 文件名 + 移除按钮），一目了然；对话里会记录一条图片消息，可被归档压缩。
- 拖拽悬停时对话框高亮提示；切到 DeepSeek 或清空对话时预览条同步移除（图片上下文不留脏）。

## 0.26.0 — 2026-08-04

- 落盘防御加固：新增通用守卫 `ensureVaultFileParent`（写文件前确保父目录存在，`vault.create` 不自动建父目录）+ `writeVaultFile`（存在则追加、不存在则带标题创建）。
- 强制走守卫的三条路径：AI 归档（`attachments/jam-deck-chatbot/`）、AI 对话记录（`Work/AI对话记录.md`）、画笔笔迹保存（CanvasInkOwner.writeText）。
- 外部资源 validate 维持现状（拖入/读取前已做存在性 + 大小限制检查），本轮聚焦落盘侧。

## 0.25.5 — 2026-08-04

- 修复 AI 助手归档失败：首次归档时 `attachments/jam-deck-chatbot/` 子目录未建，`vault.create` 不会自动建父目录，抛 ENOENT。归档写入前显式 `ensureVaultFolder("attachments/jam-deck-chatbot")` 兜底。

## 0.25.4 — 2026-08-04

- 修复从剪贴板拖图片到 Canvas 失败：0.25.2 队列化重构时把 clipboard 拖拽源多取了一层 `.item`（`source.item`），但 `getClipboardCanvasDrop` 返回的 `items` 数组元素就是剪贴板条目对象本身，再 `.item` 就是 `undefined`，导致 `createCanvasAttachmentFromClipboard` 抛「剪贴板图片无效」。改为直接传 `source`。external 路径不受影响。

## 0.25.3 — 2026-08-04

- 大幅降低大图量 Canvas 平移卡顿：选中工具栏同步在鼠标按下（平移/拖拽）期间暂停，松手再补一次；选中节点检测从两次全量遍历合并为一次。图片/节点很多的画布（如 S5 赛季 guide）拖动明显更顺。
- 多图拖入自动排布：一批拖入多张图时，第 1 张落在鼠标位置，后续各张自动横排到上一张右侧（同宽高、世界坐标 28px 间距），不再全部叠在同一个点上。
- 多选节点时工具栏按钮仍不显示（语义不变）；单图拖入行为不变。

## 0.25.2 — 2026-08-04

- 修复 Canvas 不能一口气拖入多张图片：拖入（含系统文件多选）改为**队列串行**处理，每张依次复制为附件并建节点，不再因上一张仍在写入而拒绝（原来的「上一张图片仍在写入」锁已移除）。
- 批量性能：中间图片只做合并保存（requestSave），仅对队列尾统一强制落盘（saveImmediately）+ 选中，避免连续多张反复全量序列化导致卡顿；完成后弹一次汇总提示（成功/失败张数）。
- 外部拖拽现在收集 transfer 里**全部**图片文件（含 file:// uri 去重补充），不再只取第一张。

## 0.25.1 — 2026-08-04

- 修正 AI 助手标题栏按钮布局：归档 / 清理 / 关闭三个按钮打包为右侧操作组（`jam-deck-ai-chat-actions`，组内 4px 间距），紧贴关闭按钮左侧排列，不再被 `space-between` 散开到标题栏中部。

## 0.25.0 — 2026-08-04

- AI 助手新增**归档**与**清理**两个管理动作（标题栏模型按钮旁）：
  - **归档**：把当前窗口尚未归档的对话转成纯文本，由 DeepSeek 压缩成精炼纪要（保留关键事实/决定/待办与画布操作/图片主题，150 字内），追加写入 `attachments/jam-deck-chatbot/YYYY-MM-DD.md`（按日期分档，`## HH:mm` 一段一条）。已归档的对话不清理、但靠游标去重——下次归档只记录新增部分。
  - **清理**：清空当前对话窗口上下文（含输入框），不影响任何已归档记录。
- 归档固定走 DeepSeek 端点（不管当前模型是千问还是 DeepSeek）；未配置 DeepSeek Key、无新增对话、压缩或写入失败均有明确提示。

## 0.24.2 — 2026-08-04

- 修复 AI 助手切换模型后的误拦截：对图片节点打开过 AI（千问看图）后切到 DeepSeek，再发纯文本会误报「看图需要千问」。
- 原因：图片上下文（base64）一直挂在会话上不释放，纯文本也被当作看图请求。
- 修复：切换到 DeepSeek 时自动降级图片上下文（保留选中的节点上下文，纯文本对话继续），并提示需要看图时重新对图片节点打开 AI 助手。

## 0.24.1 — 2026-08-03

- **对话记录持久化**：AI 对话（含翻译、图片问答）自动追加到 vault 文档 `Work/AI对话记录.md`（时间戳 + 角色 + 内容，AI 侧标注模型），便于回溯。
- **聊天窗口跟随悬浮按钮**：AI 窗口始终贴在悬浮按钮旁边（右侧优先，放不下自动换左侧），拖按钮窗口跟着走。
- **双向联动拖拽**：拖动聊天窗口头部同样移动悬浮按钮（共用同一位置状态，位置持久化到设置）。

## 0.24.0 — 2026-08-03

- **AI 悬浮按钮可自由拖拽**：按住拖动即可移动位置（pointer 拖拽 + 边界约束），位置跨界面重建保留，拖拽后可点击照常开关对话窗。
- **对话消息可选中 / 复制**：消息文本显式 `user-select: text`；每条消息 hover 出现复制按钮（copy 图标），点击一键复制到剪贴板。

## 0.23.4 — 2026-08-03

- 修复图片对话 failed to fetch（真正根因）：Obsidian 渲染进程的 fetch 流式在部分环境不可用（CSP/网络栈拦截），此前流式翻译同样受影响。所有 AI 流式通道统一改回 **requestUrl**（Obsidian 主进程网络栈，稳定可靠）：`streamChat` 内部改为非流式 requestUrl，`onChunk` 一次回调全文，调用方增量渲染写法不变。图片对话与翻译恢复可用（失去打字机效果，换取稳定性）。

## 0.23.3 — 2026-08-03

- 修复图片对话 `failed to fetch`：原始 base64 body 过大（10MB+）导致上传超时。发送前用 canvas 压缩图片（最长边 2048px，PNG/WebP 保持格式、JPEG 白底 + 0.85 质量，body 通常 <2MB），识别效果不受影响；流式请求增加 90s 超时与更明确的错误提示。

## 0.23.2 — 2026-08-03

- 修复千问 401 invalid_api_key：Token Plan 个人版专属 API Key（`sk-sp-` 开头）与百炼通用 key（`sk-`）及 Base URL 完全隔离，不可混用。插件现在**按 key 前缀自动路由**——`sk-sp-` 开头走 Token Plan 专属端点 `token-plan.cn-beijing.maas.aliyuncs.com/compatible-mode/v1`，`sk-` 走通用端点。设置面板补充说明。

## 0.23.1 — 2026-08-03

- 千问模型默认切换为 **qwen3.8-max**（2026-08-03 刚发布的最强旗舰，原生多模态；下拉含 qwen3.8-max-preview / qwen-vl-max / qwen-vl-plus / qwen3-vl-plus）。
- AI 注入身份认知：system prompt 告知当前运行模型（DeepSeek / 千问 + 模型名），回答"你是什么模型"时如实作答，不再答"未公开"。

## 0.23.0 — 2026-08-03

- **接入千问（通义千问）多模态**：设置面板新增千问 API Key 与模型选择（qwen-vl-max 默认 / qwen-vl-plus / qwen3-vl-plus），DeepSeek 栏位独立保留。
- **模型切换按钮**：AI 对话窗标题旁新增 DS / 千问 切换胶囊按钮（设置面板也可设默认），provider 路由统一（baseUrl / key / model）。
- **图片发送到对话**：Canvas 选中图片节点时 AI 按钮同样可用——点击把图片（vault 读取转 base64，≤15MB）载入对话窗，自动切千问，多模态流式问答（描述 / 配色 / 构图 / 风格）；消息区显示图片缩略图。
- 代码重构：`getAiConfig()` provider 路由、`streamChat()` 公共流式方法（SSE）、`streamTranslate` / `streamChatWithImage` 复用。

## 0.22.1 — 2026-08-03

- 修复组件间隔悬停绿点经常失效：sash 圆点位置只在渲染时计算一次，窗口/面板缩放、图片加载、canvas 挂载、compact 切换后坐标过期导致悬停无响应（进编辑模式触发重渲染才恢复）。新增 ResizeObserver（观察 grid 与每个 widget）+ window resize 监听，rAF 防抖自动重算圆点位置。

## 0.22.0 — 2026-08-03

- **翻译大幅提速**：语种快捷按钮改走独立流式翻译通道——不打包 JSON、不注入待办上下文（省 token 与首字延迟），SSE 流式输出边生成边显示（打字机效果），翻译完成直接创建 Canvas 节点。
- **联网搜索**：接入 DeepSeek function calling——AI 判断需要实时信息或用户明确要求搜索时，自动调用 `web_search` 工具（DuckDuckGo 主源 / Bing 兜底），两轮对话完成，reply 中总结搜索结果。

## 0.21.3 — 2026-08-03

- 修复 AI 翻译长文本被截断：注入 AI 的选中文本上限从 300 字符放宽到 8000 字符（原文过长时只翻译了开头）；API 请求增加 `max_tokens: 8192`，保证长翻译输出完整。

## 0.21.2 — 2026-08-03

- AI 翻译加速：选中 Canvas 文本节点点 AI 按钮后，对话窗自动弹出四个语种快捷按钮（中文 / 英文 / 韩文 / 日文），点击即发起翻译，无需打字；有其他要求仍可从输入框直接输入。

## 0.21.1 — 2026-08-03

- 修复 addCanvasText 创建翻译节点失败：`createTextNode` 的 `size` 参数误用 `{x, y}` 格式（Obsidian 内部为 `{width, height}`），导致部分节点以 0,0,0,0 落位且渲染卡顿。
- 创建后校验节点已正确落位（宽高 > 0），异常节点立即从画布移除，不再残留幽灵节点；`save: false` + 统一 `requestSave`，避免连续创建时重复保存冲突。

## 0.21.0 — 2026-08-03

- Canvas 选中工具栏行为修正：Eagle 以图搜图按钮只在选中**图片节点**时出现（改用内联 display 控制，避免原生 `.canvas-menu` 样式覆盖 `[hidden]` 导致文本节点选中时按钮残留）。
- 选中**文本节点**时，工具栏新增 AI 按钮：点击把选中文本载入 AI 对话窗，用户补充目标语种后，翻译/改写结果以**新 Canvas 文本节点**贴在原节点右侧或下方（AI 返回 `addCanvasText` 操作，插件原生创建节点并保存）。

## 0.20.2 — 2026-08-03

- AI 对话窗尺寸翻倍（340×480 → 680×780，受屏幕空间上限约束自动收缩）。
- AI 助手明确能力边界：开发/改插件/跑命令类请求不编造，reply 引导用 WorkBuddy 完成，并可选地把需求转述为待办加入列表。

## 0.20.1 — 2026-08-03

- AI 助手重构为悬浮对话窗（取代 0.20.0 的主功能栏展开面板）：右下角悬浮 AI 胶囊按钮常驻，点击开合 340×480 对话窗。
- 对话窗包含消息列表（用户 / AI 气泡，含执行结果汇总）、auto-grow 输入框（上限 160px）、发送按钮；AI 回复可见可滚动，历史消息跨界面重建保留。
- DeepSeek 返回结构增加 `reply` 人类可读总结字段；纯提问（无操作）不再报错，直接回复。

## 0.20.0 — 2026-08-03

- 主功能栏新增 AI 对话入口（第五入口）：点击展开极简输入面板，输入框高度随内容自适应（上限 160px），Enter 发送 / Shift+Enter 换行。
- 接入 DeepSeek V4 API（OpenAI 兼容）：设置面板新增 API Key 与模型选择（deepseek-v4-flash 默认 / deepseek-v4-pro），对话直接以插件原生方法新增、完成、删除待办，落盘与界面刷新与手点操作同路径。
- 操作执行前自动注入当前待办与日期上下文，模型仅返回结构化 JSON（addTask / completeTask / deleteTask，≤20 条/次），执行结果即时同步待办列表与日历。

## 0.19.6 — 2026-08-02

- 修复 Obsidian 中 CSS 外链背板未实际渲染：文件夹现在直接挂载 Figma 路径的内联 SVG，并锁定背板 → 代表图 → 前片 → header 层序，避免原生 Canvas 改写节点 z-index 后错层。

## 0.19.5 — 2026-08-02

- 再次按 Figma `NZS4 / 102:6` 校正折叠文件夹：彻底移除额外遮罩 DOM/CSS，层级收敛为完整 SVG 底板、真实代表图、半透磨砂前片和文字/双凹槽。
- 底板恢复导出 SVG 的 `240×181.79` 原始溢出尺寸，不再把透明阴影边距压进 `200×141.79` 容器；使用 SVG 自带的路径阴影，避免矩形 CSS 阴影破坏底部圆角。
- 图片节点、容器、媒体层与图片本体统一使用共享 10px 圆角并逐层裁切；前片保持 `200×100`、四角 10px，旧堆叠展开、悬停抬起、拖出和颜色列表交互不变。

## 0.19.4 — 2026-08-02

- 文件夹折叠态补齐 Figma `NZS4 / 102:6` 的显式背板、代表图、遮罩、磨砂前片和双凹槽层序；四种代表图状态使用设计节点的精确坐标、尺寸与旋转，颜色圆钮展开 6 色列表，不再出现矩形背板或不透明封面。
- 文件夹继续复用旧堆叠预览：点击时前片向外翻开并让卡片散开，收起时卡片先回位、再合拢前片；材质与节奏参考 `fayazara/portfolio-site-template` 的半透模糊 Folder，拖出、取消、Esc、空白点击和减少动态效果均有完整清理路径。
- 原生 Canvas 打开同一路径时改为 quiet teardown：只停止 Jam Deck 自有 leaf、监听器与异步操作，不调用原生 Canvas 的保存、关闭或 workspace 激活接口；高频 workspace 事件合并为单 timer/串行 reconcile，最后一个原生 leaf 关闭后只恢复一次。
- 图片拖入与 Eagle 搜索纳入销毁屏障，冲突暂停会先 abort 并等待在途任务，再卸载 owned leaf，避免原生 Canvas 编辑期间仍有后台节点写入、保存或通知穿透。

## 0.19.3 — 2026-08-02

- 文件夹折叠态严格采用 Figma `NZS4 / 102:6` 的 200×150 比例与导出背板：前片为 200×100、10px 圆角、指定灰白渐变和上缘阴影，文字按 12px 左边距分层，只保留右下颜色圆钮与两道凹槽；彩色预设只做轻量染色。
- 文件夹只替换旧堆叠的收拢外观：点击壳体恢复原堆叠展开预览，预览卡片仍可点击、聚焦和拖出；修复节点 pointerdown 中未定义变量导致的悬浮抬起、点击展开与手拖自动成组整体失效。
- 折叠文件夹不再继承宽扁图片的尺寸，而是按 Canvas 缩放使用 200×150 基准并以锚点居中；网格排列按钮改用 Obsidian 实际提供的 `layout-grid` 图标。部署脚本同步受保护发布 Figma SVG 资产，继续禁止覆盖 `data.json`。

## 0.19.2 — 2026-08-02

- 修复折叠文件夹缩略图在小尺寸 Canvas 节点上被封面最小高度完全遮住的问题：封面继续占下方 58%，但始终为真实成员缩略图保留至少 28px 的露出区域。
- 折叠、展开与收拢阶段优先使用代表节点进入展示变换前捕获的屏幕矩形来定位文件夹壳体，避免已旋转/缩放的成员几何反向参与壳体计算；恢复展开态时同步清理捕获矩形。
- 补充壳体稳定边界与紧凑高度封面的回归契约；排查 Jamnote 启动闪退时未发现 Vault 损坏，重新启用 Jam Deck 后原测试 Canvas 可稳定加载和继续编组。

## 0.19.1 — 2026-08-02

- 完成 Canvas 文件夹的稳定展开/收拢状态机：展开 300ms、收拢 260ms，成员错峰 18ms（最多 72ms），并在减少动态效果或 WAAPI 不可用时直接落到最终状态。
- 折叠文件夹改用按稳定 ID 复用的 keyed 壳体与单一 leaf 内颜色 popover；预览继续使用真实 Canvas 成员，不克隆或重新挂载节点。展开布局为 2–4 个成员两列，5 个及以上三列（6 个为 3×2），折叠代表预览仍限制为单列/双列。
- 聚焦仅由显式按钮发出 runtime focusRequestToken，展开完成后消费一次并过滤最新成员；schema v1 与 Canvas `jamdeck` 元数据边界保持不变，事务失败继续整组回滚。

## 0.19.0 — 2026-08-02

- 新增 Canvas 文件夹编组：手拖节点覆盖另一成员超过较小节点面积 50% 时自动建组；多选工具栏提供“堆叠编组”和“网格排列”。
- 文件夹成员在 Canvas 节点 `jamdeck.folderId` 与锚点 `jamdeck.folder` 中保存；新组默认折叠，壳体可移动、展开、循环 6 色、聚焦或取消编组，最多显示 4 个真实代表成员。
- 编组、移动、布局、颜色和折叠状态均通过一次 Canvas 保存与原生历史提交，失败时整组回滚；显式文件夹与旧隐式堆叠分开处理，作用域严格限制在内嵌 `jam-deck-canvas-leaf`。

## 0.18.5 — 2026-08-02

- 接管 Eagle/文件管理器拖入嵌入 Canvas 的图片：先复制到 vault 的 Canvas 附件目录，再创建图片节点，不再让原生拖放处理器与 Jam Deck 同时写入。
- 外部图片导入增加 64MB 上限；空的 `.canvas` 文件直接暂停嵌入，避免保存截断后不断重试导致 Obsidian 卡死。

## 0.18.4 — 2026-08-02

- 修复 Obsidian 1.13 工作区集合不是普通数组导致的误判：原生 Canvas 现在会被可靠识别，Jam Deck 不再与原生页面重复持有同一 Canvas。
- 原生 Canvas 打开/关闭时只就地暂停或恢复对应嵌入组件，不再重建整个 Jam Deck，避免切换期间出现渲染递归和内存暴涨。

## 0.18.3 — 2026-08-02

- 修复同一 Canvas 同时在 Jam Deck 和 Obsidian 原生页面打开时的重复渲染竞争：检测到原生 Canvas 后，Jam Deck 自动暂停该嵌入视图并显示轻量提示；关闭原生页面后自动恢复。
- 监听工作区布局和活动页面变化，并保留原生 Canvas 的单一编辑实例，避免两套节点渲染、文件监听和拖拽状态互相争用。

## 0.18.2 — 2026-08-02

- 修正以图搜图入口位置：改挂到 Obsidian 原生 `.canvas-menu` 选中节点工具栏，不再写入底部 `.canvas-card-menu` 新建节点工具栏。
- 搜索按钮改用原生 `clickable-icon` 外观，沿用选中工具栏自身的尺寸、间距、阴影和明暗主题。

## 0.18.1 — 2026-08-02

- Canvas 图片以图搜图入口移入选中图片上方的原生悬浮工具栏，不再在图片右上角额外生成悬浮圆钮；仅单选图片时显示并保持与画笔按钮一致的工具栏尺寸、阴影和明暗主题。
- Eagle 搜索结果上限从 20 张收敛为 10 张；结果沿用源图 Canvas 尺寸，固定按 5 列×2 行排在源图下方，不再形成堆叠。
- 搜索结果批量插入仍合并为一次 Canvas 历史，任一步失败继续回滚已创建节点。

## 0.18.0 — 2026-08-01

- 内嵌 Canvas 图片节点悬停时，节点右上角出现「Eagle 以图搜图」圆形按钮；点击后把图片字节直接发给本机 Eagle ai-search 服务（固定端口 38766），取相似度最高的前 20 张。
- 结果按各图真实宽高比等宽排布，全部落在原图右侧 40px 的同一位置，天然形成可单击展开的堆叠；整批插入合并为一次 Canvas 历史，任何一步失败回滚已创建节点。
- 素材本体仍由 Eagle 管理：结果直接引用 vault 内 `JAM收集.library` 的原始文件，不复制副本；Eagle 未启动、搜索服务未就绪或无结果时只以通知提示，不改动 Canvas。

## Game Deck 0.3.0 — 2026-08-01

- 网格改为 32×18、每格 1×1 世界单位（正方形），整片地块正好 16:9，与固定镜头画面一致。
- 最小占地改为 2×2；默认房屋 / 箱子 / 音乐盒尺寸与位置按新网格重排。旧 24×16 布局不迁移坐标，升级时重置为默认排布。
- 新增 Blender 5.2 源文件 `game-deck/assets/game-deck.blend`：按 `GameDeck → grid / props / foliage / scenery / terrain / lighting` 分集合，各方块占位示意；可用 `build_blend.py` 重建。

## Game Deck 0.2.0 — 2026-08-01

- 镜头改为固定：正前方 48° 俯视，按窗口比例二分出刚好罩住整块地的距离，窗口变化时重新取景，不再自由旋转。
- 首屏卡顿修复：地面细分 190→110、草叶 5.8 万→3.2 万（地块区域密度仍是 15 根/㎡）、每叶顶点 10→8，阴影贴图从逐帧重算改为按需重烤，渲染像素比上限 1.75→1.35。
- 草叶按由近到远排序命中 early-z；树冠与云各自合并成单个几何体、石头改为实例化，场景 draw call 从约 160 降到约 60。
- 建场景前先画出「正在生成草原…」并预编译 shader，打开视图不再整块假死。

## 0.17.3 — 2026-08-01

- 修正外边界交点语义：落在单个组件真实右下角的小绿点只改变该组件宽高，不再联动同一行或同一列的其他组件。
- 右/底边缘中段圆点继续负责整条边界联动，普通内部十字交点继续调整四周组件，三类控制点职责分离。
- 单组件角点缩放保持 2×2 机械下限、画布上限与全局碰撞校验；无法向已有组件方向扩张时保持原布局。

## 0.17.2 — 2026-08-01

- 工作台右外边界、底外边界及右下角纳入绿色圆点系统；对应内部横缝/竖缝的交点也补齐，可整体向内收缩贴边组件并腾出连续空白区域。
- 外边界圆点跟随当前组件包围盒移动，向内收缩后不会消失，可继续收缩或反向扩回；仍受 2×2 机械下限和画布边界约束。
- 添加组件先寻找默认尺寸空位，再寻找该类型最小完整尺寸空位；仍无空间时自动选择当前占地面积最大的可压缩组件，在其右侧或下方切出最小完整区域插入新组件。
- 自动插入只压缩选中的最大组件，不移动无关组件；没有任何组件能继续压缩时才提示无法让位，保存失败则恢复原布局。

## 0.17.1 — 2026-08-01

- 编辑态拖动组件时新增自由空放：鼠标所在空白矩形只要能容纳组件原尺寸，即可直接预览并松手放置，不再仅接受组件间填充槽或零缝推挤点。
- 自由空放保留组件原宽高和按下时的抓取位置，不改动其他组件；确实放不下时才继续尝试矩形填充或推挤。
- Shift 仍保持“延伸填充到画布边缘”的显式优先级，不受自由空放覆盖。

## 0.17.0 — 2026-08-01

- Game Deck 从 Jam Deck 里分家：成为独立插件（id `game-deck`，源码在 `game-deck/`，独立 manifest、样式与 `data.json`）。
- Jam Deck 回到纯 2D 工作台：移除 `GameDeckWorldView`、骰子入口、`game-deck-world.js` 打包产物与相关样式。
- 新增 `npm run build:game-deck` / `npm run deploy:game-deck`；`verify` 同时检查两个插件，`deploy` 仍只发布 Jam Deck。
- Game Deck 首版（0.1.0）：Tiny Glade 风草原，房屋 / 箱子 / 音乐盒三种 3D 物件，instanced 草叶的风吹草动循环；点「编辑地块」切到 2D 网格用同源的填充 / 推挤 / Shift 贴边 / 间距节点排布，点「完成」回到 3D。

## 0.16.2 — 2026-08-01

- 点击紧凑组件水印恢复时，优先移动它与下方或右侧组件的共享边界；目标组件长回完整显示范围，相邻组件同步压缩，不再因“只允许整块搬移”而误报空间不足。
- 只补足低于阈值的方向：组件已经足够宽或足够高时保留现有尺寸，不会为了恢复另一方向反而缩回阈值。
- 共享边界无法提供足够尺寸时才回退到原有整块自动让位；两条路径都保持碰撞校验与失败回滚。

## 0.16.1 — 2026-08-01

- 编辑模式移除组件右下角斜杠缩放手柄，改为与浏览模式一致的间距绿色圆点；竖缝、横缝和十字交点均可直接调整相邻组件。
- 圆点拖动的 DOM 写入与几何重算合并到动画帧，减少高频 pointermove 造成的卡顿。
- 紧凑组件正文改为保持热挂载：拖动跨过该类型完整显示阈值时立即从水印恢复正常内容，不再等松手保存后才首次渲染。

## 0.16.0 — 2026-08-01

- GameDeck 分支实验：新增 Three.js「Game Deck」世界视图（草地场景 + 音乐盒 / 日历 / 图片 / 文本占位模型）。
- 支持点击选中、地面拖动摆放、Alt+拖动拉伸；侧栏骰子图标与命令 `Open Game Deck world` 可打开。
- 世界源码在 `game-deck/world.js`，经 esbuild 打包为 `game-deck-world.js`；原有 2D Jam Deck 工作台仍可用。

## 0.15.0 — 2026-08-01

- 以当前九类组件的实际排布尺寸记录各自“完整显示阈值”；任一方向低于阈值时，只保留居中的标题图标水印，不再挤压正文。
- 点击水印会把组件恢复到该类型的最小完整尺寸，并自动推挤冲突组件；同时冲突时优先让占地面积最大的组件移动。
- 自动让位优先减少受影响组件数量和总位移，未进入碰撞链的组件保持原位；无空间、搜索达到保护上限或保存失败时整次回滚。
- 紧凑预览不提前销毁 Canvas、浏览器或播放器资源，布局提交后才按最终状态重建；完整回归通过且部署不覆盖个人 `data.json`。

## 0.14.0 — 2026-08-01

- 非编辑态支持间距节点：鼠标靠近组件交界节点时出现小圆点，可沿缝拖动，统一移动边界并自适应两侧组件面积。
- 十字交点可四向拖动，竖缝左右拖、横缝上下拖；贴齐的同列/同行边界会合并为一条缝，一次拖动整列一起变。
- 圆点默认不抢点击，只有靠近节点时才激活；编辑模式仍用原有拖动与缩放。

## 0.13.9 — 2026-08-01

- 拖动组件时底部出现白色渐变提示条，文字说明「按住 Shift 可延伸填充到画布边缘」。
- 按住 Shift 后提示立即消失；松手结束拖动时一并收起。

## 0.13.8 — 2026-08-01

- 拖动时 A 立即收缩到最小尺寸（2×2）参与插入计算与悬浮预览；推挤插入也按最小宽/高让位，不再按 A 当前尺寸推开邻居。
- B 到画布右侧/底部、没有后邻 C 的延伸矩形填充改为按住 Shift 才触发；普通拖动仍只处理 B/C 之间的空隙填充与零缝推挤。
- 拖动过程中按下或松开 Shift 会即时刷新预览，无需重新拿起组件。

## 0.13.7 — 2026-08-01

- 网格改为 40×36，1920×1080 下每格约 37×23px，横向和纵向都能细调；现有组件坐标原样保留，可自行重排。
- 网格行高下限从 28px 降到 12px，36 行不会撑破工作台高度。
- 缝隙命中带按新格距调整为 2.5 格，保持与之前相近的手感；新增组件默认尺寸按 40×36 重新给定。

## 0.13.6 — 2026-08-01

- 网格列数由 12 提升到 24，横向步进从约 133px 缩小到约 65px；1920×1080 下每格接近正方形（65×50px），拖动和缩放都能精细调节。
- 行数保持 18 不变，已有布局在升级时按 ×2 自动迁移列坐标，视觉位置和大小完全不变（`dataVersion` 升到 4）。
- 新增组件的默认宽度同步翻倍，保证与旧版本占位一致。

## 0.13.5 — 2026-08-01

- 修复贴边组件挡住推挤插入：邻居无处可移时改为缩短宽度或高度让位，例如时钟与快捷方式之间插入日历时快捷方式自动变窄。
- 缝隙命中范围从 0.85 格放宽到 1.5 格，拖到两组件交界附近即可亮起两端渐变，不必精确对准 1 格。
- 推挤链改为只影响与缝真正相邻的一列或一行组件，其余布局保持不动。

## 0.13.4 — 2026-08-01

- 编辑拖动同时支持两种落位：悬停在可容纳的空隙上按绿色矩形填充插入；B/C 之间没有空隙时改为亮起两端绿色渐变并推挤插入。
- 推挤插入保持 A 的原有高度或宽度，交叉轴对齐 B/C；C 及后续组件整体下移或右移，尺寸不变。
- 拖动过程中被推挤的组件实时预览新位置，松手一次提交；两种模式都不需要先腾空。

## 0.13.3 — 2026-08-01

- 编辑拖动改为整块悬浮抬起，拖动过程不算碰撞；松手只按鼠标悬停的空隙插入。
- 可容纳最小宽高的 B/C 空隙会预绘绿色渐变描边矩形；纵缝按统一宽度填满空隙，横缝按统一高度填满空隙，松手以矩形尺寸落位。
- 若没有 C，则分别向画布底部或右侧延伸填充矩形；未悬停在可填充空隙上时松手取消。

## 0.13.2 — 2026-08-01

- 宽缝可直接放下 A 时不再亮渐变，也不再把 C 吸上来；按原尺寸自由放置。
- 零间距/过窄缝恢复可插入：拖动保持悬浮，悬停缝边亮浅绿渐变，松手后插入 A 并推动 C（及后续）下移或右移，保留其原有尺寸。

## 0.13.1 — 2026-08-01

- 修正编辑拖放交互：拖动中组件保持原尺寸并带悬浮阴影，不再整带等分。
- 落到等宽纵向缝时，B 底/C 顶亮起浅绿渐变；松手后 A 以 B/C 宽度、最小高度插入，C 及后续 D/E 次第缩短高度让位。
- 落到等高横向缝时，B 右/C 左亮起浅绿渐变；松手后 A 以 B/C 高度、最小宽度插入，C 及后续 D/E 次第缩短宽度让位。

## 0.13.0 — 2026-08-01

- 编辑模式下拖动组件改为推挤自适应布局：插入同行横缝时整带均分宽度，冲突组件自动下推，无需先腾空再放置。
- 拖动过程实时预览邻居缩短与让位，松手一次提交完整布局；空间不足或超出 18 行时保留红框并回滚。
- 插入位置使用 1–2px 低对比细线指示；减少动态效果时跳过过渡但仍呈现最终布局。

## 0.12.5 — 2026-07-31

- 按用户截图重排歌曲信息：唱片保持左侧，歌名和歌手回到唱片右侧的主体中部并左对齐，不再锚定组件最右边。
- 宽组件将唱片与文字间距调整为 24px；窄组件继续使用紧凑间距和安全边距。

## 0.12.4 — 2026-07-31

- 歌曲信息不再依赖剩余网格空间，自身锚定在主体区右侧并垂直居中；文本区宽度限制为主体的 44%、最大 360px。
- 窄组件中歌曲信息使用唱片右侧的实际剩余宽度，继续保持右侧 8px 安全边距和右对齐。

## 0.12.3 — 2026-07-31

- 为播放器主体增加响应式安全边距，唱片不再贴近左侧和顶部，右对齐的歌名与歌手不再贴住组件右边缘。
- 标准宽度使用 12px 横向、8px 顶部留白；窄组件自动收为 8px 横向、6px 顶部，避免挤压歌曲信息。

## 0.12.2 — 2026-07-31

- 音源按钮脱离歌曲信息区，固定到播放器右上角；歌名和歌手位于组件右侧并统一右对齐。
- 常态下移播放进度区，去掉隐藏控制键留下的空白，唱片、歌曲信息与时间线之间获得更舒展的安全距离。
- 上一首、播放/暂停、下一首改为覆盖在进度区上的悬浮层，不再参与常态排版；悬浮层使用随明暗主题变化的底部渐变遮住进度条，避免按钮被组件边缘裁切。

## 0.12.1 — 2026-07-31

- 音乐播放器改为轻量布局：移除歌词、可见来源状态和收藏，歌名/歌手固定在 CD 右侧，音源收为 20px 小按钮。
- 新增白色唱臂；只在系统确认播放时落针，暂停、停止、启动等待或无会话时抬起，并遵循减少动态效果。
- 上一首、播放/暂停、下一首只在组件悬停或键盘焦点进入时浮现；触摸首次聚焦同样可唤出。
- 进度条改为可访问的 Range Slider；播放器公开 Seek 能力时支持鼠标、触摸和键盘拖动，松手只发送一次并等待权威位置确认。
- 无受支持媒体会话时，播放键可启动上次确认连接的 QQ 音乐、网易云音乐或汽水音乐；启动通过严格白名单和 Windows 已注册 Shell 项完成，不接受任意路径、参数、命令或 URL。
- 同源多会话会被标记为歧义并拒绝控制；浏览器媒体会话不会替代三款受支持的音乐音源。

## 0.12.0 — 2026-07-31

- 新增 Windows 音乐播放器组件：通过系统 GSMTC 会话读取本地播放器的歌名、歌手、专辑、封面、播放状态和时间线。
- 封面以旋转 CD 呈现，播放时转动、暂停时停驻；组件包含进度、音源选择、上一首、播放/暂停、下一首和 Jam Deck 本地收藏。
- 支持识别 QQ 音乐、网易云音乐和汽水音乐的系统媒体会话；按真实会话和能力启用按钮，不伪造不可用控制。
- Windows 系统媒体接口不提供通用歌词，因此保留歌词位置并明确显示不可用，不抓取播放器窗口或私有缓存。
- 使用单一持久媒体桥和低频轮询，页面可见播放时 2 秒、暂停时 5 秒、隐藏时 15 秒采样，界面进度在两次采样之间本地平滑推进。
- 媒体桥使用固定 UTF-16LE 编码脚本和 JSONL 协议，动态音源仅通过标准输入传递；封面限制大小、类型与缓存总量，卸载时完整释放。

## 0.11.14 — 2026-07-31

- 组件标题栏操作按钮由 24–25px 大圆钮改为接近 Beta 标记的 16px 轻量胶囊样式。
- 按钮统一使用 8px 字号、细描边、透明底与轻微悬停反馈，避免超出 31px 标题栏的视觉高度。

## 0.11.13 — 2026-07-31

- 顶部品牌名、副标题、组件标题和日历区间标题改用工业信息界面字体栈：拉丁数字优先 Bahnschrift，中文回退 Microsoft YaHei UI。
- 标题字重统一为 600，副标题为 400；取消小字号过度字距和字体合成，并启用几何精度与抗锯齿渲染。
- 日历区间数字启用等宽数字特性，提升日期排列的工整度。

## 0.11.12 — 2026-07-31

- 滚动条滑块默认视觉宽度保持 2px、强度调整为 20%。
- 滑块悬停时透明边框收起，视觉宽度扩展至 6px，强度提升至 40%；轨道和上下按钮继续隐藏。

## 0.11.11 — 2026-07-31

- 修复 Chromium 标准 `scrollbar-width: thin` 覆盖精确 WebKit 样式，导致滚动条宽度、箭头和悬停强度没有变化的问题。
- 移除冲突的标准滚动条声明，将 2px、无轨道、无按钮样式放到样式表末尾并提高优先级。
- 滑块基础视觉强度降为约 18%，悬停为约 23.4%，相对增强 30%。

## 0.11.10 — 2026-07-31

- Jam Deck 滚动条进一步收窄至 2px，并隐藏 WebKit 滚动条上下箭头。
- 滑块悬停强度由基础 50% 提升至 65%，相对增强约 30%。

## 0.11.9 — 2026-07-31

- Jam Deck 滚动条进一步收窄至 4px。
- 移除可见滚动轨道，仅保留半透明滑块及轻微悬停反馈。

## 0.11.8 — 2026-07-30

- Jam Deck 范围内的纵向与横向滚动条收窄至 6px，约为 Obsidian 默认宽度的一半。
- 滑块与轨道颜色以主题变量为基础降低至 50% 混合强度，悬停时仅轻微提升可见度；不影响 Obsidian 其他页面。

## 0.11.7 — 2026-07-30

- 将 Jam Deck 工作台组件之间的横向与纵向间距统一收紧为 4px，提升副屏信息密度。
- 将工作台组件外框圆角统一为 4px；组件内部按钮、图片、卡片与 Canvas 节点继续保持各自的视觉层级。

## 0.11.6 — 2026-07-30

- 原生 Canvas 网页节点新增同页导航桥：普通左键点击网页中的 HTTP(S) `_blank` 链接时，优先在当前 Canvas 网页表面内打开。
- 功能仅作用于 Jam Deck 托管的 `type:"link"` 节点；不全局拦截窗口，不读取网页内容、Cookie 或表单，修饰键、下载、登录脚本与非 HTTP 协议保持原行为。
- iframe/webview 注入经过精确节点映射和能力探测；跨进程子帧不可访问时安全停止，不重试、不刷新或重建 Canvas。

## 0.11.5 — 2026-07-30

- 修正 0.11.4 对原生 Canvas 网页节点类型的错误假设：除了 iframe/webview，现在也会识别 Canvas 普通 DOM 内容区内、紧邻系统浏览器打开前发生的真实点击。
- 外部浏览器弹出前即使 Obsidian 内部活动 leaf 已漂移，返回协调器仍可在无竞争输入时以 `focus:false` 重申 Jam Deck 宿主，避免关闭浏览器后 Canvas 首击失效或跳到第一篇日记。
- 普通 DOM 候选仅保留 2 秒，根容器、隐藏节点、焦点哨兵以及 Canvas 外输入不会建立候选；返回时操作其他日记或组件会取消恢复。
- 保持不刷新网页、不重建 detached leaf、不修改网页地址、不合成点击且不主动抢焦点。
- 新增普通 Canvas DOM 节点、活动 leaf 提前漂移、根容器拒绝和外部竞争输入取消回归。

## 0.11.4 — 2026-07-30

- 修复内嵌 Canvas 网页节点打开默认浏览器后，返回 Obsidian 时网页和 Canvas 表面可能无法点击的问题。
- 新增按窗口隔离的 Canvas 返回协调器：只在离开前焦点属于 iframe/webview 且 Jam Deck 宿主是活动页时记录恢复候选，返回后受控重申宿主 leaf。
- 恢复使用 `focus:false`，不刷新网页、不重建 detached leaf、不修改 iframe 地址，也不主动聚焦 iframe；网页会保留原加载状态。
- 返回后的鼠标、键盘或其他叶焦点操作优先，任何竞争输入都会取消陈旧恢复；park、重新挂载、跨生命周期销毁均使待执行任务失效并清理窗口监听。
- 补充外部返回单次恢复、竞争取消、iframe 内部假失焦和全局监听清理回归。

## 0.11.3 — 2026-07-30

- 倒计时输入拆为“时 : 分 : 秒”三个两位数字格，分别提供可访问名称，修改后自动规范为两位显示。
- 运行翻牌固定为六位 `HH:MM:SS`，牌面压低至 26–30px，并缩小阴影范围，避免贴近组件底部和右边缘。
- 倒计时行增加左右与底部安全边距，窄组件下进一步压缩间距但不缩小输入命中区。
- 浅色主题采用浅纸灰牌面与深色数字，暗色主题采用炭灰牌面与浅色数字；边界、中线、阴影和输入底板均由两套独立 token 控制。

## 0.11.2 — 2026-07-30

- 倒计时运行状态改为独立数字翻牌：深色圆角牌面、上下明暗、中线和柔和阴影共同表达机械翻页层次。
- 只有变化的数字执行 280ms 短翻转；系统启用“减少动态效果”时取消动画但保留翻牌造型。
- 日期文字提升 1px，倒计时标签和可编辑时间分别提升 2px，改善副屏距离下的可读性。
- 未运行时继续保留直接可编辑时间；勾选后自动切换为翻牌显示，不增加新的操作层级。

## 0.11.1 — 2026-07-29

- 修复 Windows 下浏览器式 `window.Notification` 对象创建成功、但通知仍可能被系统静默丢弃的问题。
- Windows 通知改用 Windows Runtime Toast API，并明确绑定 Obsidian 已注册的 `md.obsidian` AppUserModelID。
- 原生通知命令以隐藏、非交互 PowerShell 子进程执行，不弹出终端窗口；失败时继续回退为 Obsidian 通知。

## 0.11.0 — 2026-07-29

- 时间组件新增可勾选启停的倒计时，支持分钟、`MM:SS` 与 `HH:MM:SS` 三种输入形式。
- 倒计时保存绝对截止时间，工作台未打开、组件重绘或 Obsidian 重启后仍按真实时间结算；完成状态先持久化并带并发锁，避免重复提醒。
- 到点优先调用桌面 Web Notification 桥发送 Windows 系统通知；权限被拒绝或接口不可用时回退为 Obsidian 通知。
- 控件沿用轻量单表面设计：复选框、状态文字与等宽时间输入共用一行，仅以细分隔线和品牌绿运行状态表达层级。

## 0.10.3 — 2026-07-28

- 修复堆叠展开文本仍继承 Obsidian 阅读宽度、自动居中与多层内边距，导致正文挤在卡片中间窄列的问题。
- 文本预览改用固定 16px 屏幕内边距，并重置内部 Markdown 容器的宽度、边距与标题/段落间距。
- 取消展开状态所有卡片共用的圆角外壳；图片、文本和笔记以自然矩形边界参与自适应排版。

## 0.10.2 — 2026-07-28

- 修复 Canvas 缩小时，展开堆叠中的文本因原生缩放补偿与预览卡片缩放叠加而异常放大的问题。
- 文本预览改用固定的 16px 屏幕目标字号，并按卡片最终 FLIP 比例反向补偿；卡片仍可自适应排版，文字视觉尺寸保持稳定。
- 文本卡片进入拖出状态后恢复直接 16px 显示，避免移除卡片 transform 时字号发生跳变。

## 0.10.1 — 2026-07-28

- 过大的 Canvas 文本节点现在与图片共用可逆尺寸归一化：进入堆叠时按目标成员平均尺寸缩小，安全拖出后围绕最终落点恢复原始宽高。
- 展开堆叠后，单击文本卡片会回到真实 Canvas 节点并直接进入原生编辑；单击图片会打开不超过视口 90% 的沉浸预览。
- 展开卡片采用鼠标/笔 6px、触控 10px 的点击—拖拽阈值；越过阈值后可将图片、文本或 Markdown 笔记直接拖离堆叠并落到画布。
- 拖出过程只移动临时预览副本，松手时才以 Canvas 世界坐标提交一次完整节点更新；Obsidian 1.12.7 实测图片与文本拖出均支持一步撤销/重做。

## 0.10.0 — 2026-07-28

- Canvas 图片、文本节点和 Markdown 笔记现在可以按同一套严格 `>50%` 世界坐标规则组成混合堆叠，并共同参与点击展开与收回。
- 明显大于目标组的拖入图片会按目标现有成员平均 Canvas 尺寸等比缩小；既有成员不变，图片不放大。
- 首次缩小前的 Canvas 尺寸以 namespaced v1 元数据保留；安全拖出后围绕最终落点中心恢复原尺寸。
- Obsidian 1.12.7 临时 Canvas 实测：拖入与拖出均分别由一次原生撤销/重做完整覆盖位置、尺寸和元数据。

## 0.9.8 — 2026-07-28

- 聚焦推挤从“仅图片文件节点”扩展为 Canvas 全部具备几何信息的节点，文本、Markdown 笔记、嵌套 Canvas、网页链接/浏览器卡片及图片都会参与让位与回位。
- 临时 translate 从图片内部容器提升到完整 `.canvas-node` 外层，不覆盖 Obsidian 的原生定位 transform，也能完整移动标签、内容和节点表面。
- 选中的图片堆叠仍从推挤集合中排除；节点世界坐标、Canvas JSON、撤销历史和文件内容继续保持不变。

## 0.9.7 — 2026-07-28

- 点击展开升级为完整聚焦场景：自适应排版覆盖区域附近的其他 Canvas 图片会在半透明蒙版下临时向外让位，收起后回到原始视觉位置。
- 推挤仅使用节点容器的屏幕层 CSS translate，并按 Canvas 缩放换算；不修改节点世界坐标、不保存 Canvas，也不进入撤销历史。
- 半透明蒙版成为真实交互隔离层，位于 Canvas 控件之上；被覆盖的图片、按钮、菜单、滚轮、右键与键盘快捷键均不可操作，点击蒙版或按 Esc 只会收起聚焦预览。
- 复制快捷键桥在入口检查聚焦状态，避免 `Ctrl/Cmd+C` 穿透蒙版复制底层图片；减少动态效果模式取消过渡但保留最终让位排版。

## 0.9.6 — 2026-07-28

- 修复点击展开时底层图片看似停留原地的问题：预览副本先以每张图片的精确原始矩形和 1:1 缩放完成布局提交，再在下一绘制周期进入自适应排版。
- 展开期间真实堆叠节点完全隐藏，由每一层 FLIP 副本承担从堆叠原位移出、再沿各自路径收回的连续视觉；收回完成后才恢复真实节点。
- 延迟启动加入当前预览与关闭状态校验，快速点击收起时不会被尚未执行的展开帧重新打开。

## 0.9.5 — 2026-07-28

- 修复内嵌 Canvas 点击后工作区错误聚焦到前方首篇日记：交互桥不再尝试激活脱离布局树的 Canvas leaf，而是保持可见的 Jam Deck 宿主 leaf 为活动页。
- 图片堆叠预览由鼠标悬停改为单击切换；再次单击同一堆叠、单击空白、滚轮、右键或 Esc 时收起。
- 单击与拖动共用 5px 手势阈值：指针移动一旦达到拖拽条件便立即收起预览，松开后只执行原生拖动和自动吸附，不再触发展开。
- 移除 hover 计时器、移动监听、离开宽限和预览走廊，减少误触与常驻事件开销。

## 0.9.4 — 2026-07-28

- 图片堆叠改用不重复的 5–9 屏幕像素错位槽位，第三张及后续图片不会再与第二张重合；缩放 Canvas 时仍保持可辨识的层数。
- 堆叠成员与拖动状态加入明暗主题柔和多层阴影，拖动时以轻微上浮和放大强化悬浮感，选中轮廓仍保持清晰。
- 悬停展开参照 Spatial 动效重做为“原位照片墙”：背景先转为低对比半透明纸面，图片从各自原位以 FLIP 动画展开，并按容器、图片比例与数量自适应排版。
- 展开采用 110ms 悬停意图、300ms 弹性缓出、18ms 错峰；离开有效走廊后以 260ms 反向回收。滚轮、右键、Esc、原生拖动和画笔会立即让出。
- 预览只创建短生命周期、无交互的清理副本，不移动或保存真实节点；自动吸附落位后的校验只读，异常时安全停用后续吸附，不追加历史或回滚写入。

## 0.9.3 — 2026-07-28

- 内嵌 Canvas 图片恢复与快捷方式图标一致的 10px 圆角，同时保持边到边铺满并隐藏文件名；原生分组圆角统一为 14px。
- 图片鼠标拖动增加轻量悬浮反馈；松开时与目标图片的重叠超过较小图片面积 50% 即自动吸附，连续重叠按连通关系形成堆叠。
- 堆叠悬停后以纯屏幕预览层自动排开，离开后收拢；预览不移动真实节点、不写入 Canvas，也不接管指针事件。
- 自动吸附复用 Obsidian Canvas 原生移动和历史保存节奏，支持一次撤销回到拖动前、一次重做回到最终堆叠；能力探测失败时安全降级为原生拖动。

## 0.9.2 — 2026-07-28

- 已完成与已归档截止待办不再渲染灰色小点和 `+N`；完成量改为日期按钮本身的品牌绿色圆角底板。
- 1、2、3、4、5 项及以上分别映射为 20%、40%、60%、80%、100% 五档热度；今天继续保留细描边并显示正确热度。
- 进行中与逾期待办仍保留可点击状态点，日期点击创建截止待办的交互不变。

## 0.9.1 — 2026-07-23

- Canvas 标注移除压感、倾角、合并事件采样、可变轮廓生成及每点附加字段，改为轻量的二维坐标与固定线宽 SVG 描边。
- 旧压感笔迹继续兼容读取，但统一按原笔画的基础粗细显示；下一次保存时会自然收敛为精简格式。
- 保留画笔、荧光笔、整笔擦除、颜色、三档粗细、撤销/重做、快捷键和平移让出，不牺牲常规标注能力。
- 剪贴板复制与删除操作移除粗指针常驻规则，仅在卡片悬停或键盘聚焦时出现，避免触控/数位板让 Windows 误判后按钮一直显示。

## 0.9.0 — 2026-07-22

- 快捷方式图标增加安全的同目录、同名 WebP 运行时回退；只有直接编辑保存快捷方式时才写回新路径，纯渲染不修改 `data.json`。
- 剪贴板完整网页链接、浏览器 URI list 和标准纯文本 URL 可拖入快捷方式组件，自动按域名命名并生成离线稳定图标；不联网抓标题或 favicon。
- 网页链接使用统一 HTTP(S) 规范化、去重与系统浏览器打开，拒绝凭据、控制字符和危险协议；本地文件与文件夹拖入保持原行为。
- 快捷方式支持同组件鼠标拖拽重排，以及 `Alt + 方向键 / Home / End` 键盘排序；采用细插入线、焦点保持和无障碍播报，保存失败恢复原顺序。
- 收紧快捷方式图标清理：数据保存成功后，仅清理可证明位于受管目录、由 Jam Deck 生成且不再被引用的图标。

## 0.8.0 — 2026-07-22

- 内嵌 Canvas 底部菜单升级为 Spatial 风格的大圆角连续底板，统一 46px 命中区、轻边界与柔和多层阴影，并保持所有样式严格限定在 Jam Deck Canvas 内。
- 新增矢量画笔模式：画笔、荧光笔、整笔擦除、4 色、3 档粗细、撤销、重做和完成；数位笔支持压力采样，鼠标保持固定宽度。
- 笔迹使用 Canvas 世界坐标实时渲染，支持缩放和平移；`Space` 临时导航，右键、滚轮和触控保留原生交互。
- 笔迹独立保存到 `.canvas.jam-deck.json` 伴随文件，具备临时写入校验、备份、损坏只读恢复、共享 owner、重命名迁移和废纸篓删除保护，不修改原生 Canvas 或 `data.json`。

## 0.7.4 — 2026-07-22

- Jam Deck 内嵌 Canvas 的图片节点改为边到边铺满，移除外层卡片留白、圆角和阴影。
- 图片节点文件名在内嵌工作区中隐藏；普通 Obsidian Canvas 标签页不受影响。
- 选中图片时保留贴边强调轮廓，不重新引入内层间隙。

## 0.7.3 — 2026-07-22

- 内嵌原生 Canvas 在点击或聚焦后同步为 Obsidian 当前活动视图，恢复原生键盘命令上下文。
- 单独选中图片文件节点并按 `Ctrl+C` 时，直接把真实位图写入系统剪贴板；多选、文本与其他节点继续使用 Canvas 原生复制。
- 离开 Canvas 操作工作台其他区域时，活动视图自动切回 Jam Deck，避免快捷键上下文滞留。

## 0.7.2 — 2026-07-22

- 日历固定保留过去一周、本周与未来两周，共四周；左右导航仍按周移动。
- 今天由绿色圆圈改为绿色圆角矩形，截止任务继续使用克制的小圆点提示。
- 待办标题、分类与截止日期字体稍微放大，改善副屏远距离阅读。
- 本次为视觉与信息密度调整，按要求未运行测试套件。

## 0.7.1 — 2026-07-22

- 撤回日历日期的厚重卡片式外观：日期恢复为无框透明点击区，今天使用细环，有截止任务时显示绿色小圆点。
- 待办列表移除逐条卡片边框与底色，改为轻分隔和低对比 hover。
- 工作/生活分类移到标题前方，使用克制的文字前缀；截止日期留在同一行末尾。
- 将 Spatial 新功能接入原则写入视觉规范与项目约定，防止后续功能再次破坏既定设计语言。

## 0.7.0 — 2026-07-22

- 待办详情新增工作/生活分类、截止日期、图片粘贴、归档、恢复和删除操作。
- 未显式分类时，标题含完整 `【】` 自动归为工作，其余自动归为生活；旧归档保持原工作日记位置。
- 生活待办归档到 `Life/Daily.md` 对应日期标题下的稳定任务块，支持后续编辑、恢复与同步删除，不改写原有自由正文。
- 日历升级为周一开头的固定 6×7 月视图，支持上月/今天/下月、截止任务标记和逾期提示；点击日期创建预填截止日期的待办草稿。
- 移除待办组件顶部直接新增输入，保留剪贴板和外部内容拖入创建。
- 日记同步增加按操作持久化的恢复状态及归档删除 tombstone，降低中途退出造成双写或失联的风险。

## 0.6.0 — 2026-07-21

- 待办归档时，图片先复制并校验到目标工作日记按 Obsidian 设置解析出的附件目录，再同步更新日记引用和待办路径。
- 新归档在设置提交成功后才清理可证明由当前任务独占的旧附件；失败时保留有效副本并支持幂等重试。
- 已归档任务新增图片采用相同流程；恢复不回迁，彻底删除不删除工作日记附件。
- 插件启动后会安全迁移仍引用旧任务附件目录的 v2 归档，历史源文件默认保留。

## 0.5.1 — 2026-07-21

- Canvas 接收剪贴板图片时先创建持久附件，清空临时剪贴板不再破坏白板图片。
- 加入 drop 事务、坐标同步捕获、重复操作保护与失败回滚。
- 明确深色主题自动跟随 Obsidian `.theme-dark`。

## 0.5.0 — 2026-07-21

- 工作台升级为 Spatial 白板风视觉语言。
- 剪贴板改为响应式 Polaroid 素材墙，图片完整显示。
- 加入明暗主题 token、键盘焦点路径与触屏操作入口。

## 0.4.x — 2026-07-20 至 2026-07-21

- 增加真实可编辑 Canvas 工作区。
- 完成剪贴板、待办、快捷方式的拖放与删除能力。
- 增加待办详情、图片、链接和工作日记同步归档。
- 优化剪贴板图片检测性能与局部刷新。
